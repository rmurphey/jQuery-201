$(function() {

/**
 * TASK: Use custom events to make it so that when a checkbox is clicked
 * in the #filters container, the list items in the #cast container are 
 * shown or hidden depending on whether they have a class that matches
 * the selected checkboxes.
 *
 * Then, add another container to the page that listens for an event to
 * be broadcast by the #cast container when the items it is showing change,
 * and lists the data-name attributes of the showing items. 
 */

});
